# PDF Policy

Only download open-access PDFs.

Allowed:

- direct PDF links exposed by metadata
- open landing pages that directly resolve to a PDF

Not allowed:

- bypassing paywalls
- using institutional credentials by default
- scraping protected publisher pages
